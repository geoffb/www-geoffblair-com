{% extends "base.njk" %}

{% set posts = collections[collectionKey].slice().reverse() %}

{% block page_main %}
{{ super() }}
<ul>
	{% for post in posts %}
	<li><a href="{{ post.url }}">{{ post.data.title }}</a></li>
	{% endfor %}
</ul>
{% endblock %}
